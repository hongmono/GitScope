# 업데이트 내용

## 새 기능

- **브랜치 범위 필터**: 히스토리에 표시할 브랜치를 직접 골라 범위를 좁힐 수 있습니다. 선택한 범위는 탭별로 저장되어 앱을 다시 켜도 유지됩니다.
- **커밋 본문 검색**: 검색이 커밋 제목뿐 아니라 본문까지 함께 찾습니다.
- **머지 커밋의 변경 파일 표시**: 머지 커밋을 선택하면 비어 있던 변경 파일 목록이 이제 첫 부모 기준으로 표시됩니다.
- 저장소당 최근 2,000개 커밋을 불러오며, 상한에 도달하면 목록 하단에 안내가 표시됩니다.

## 개선

- 검색어를 입력하거나 필터를 바꿀 때 커밋 목록 계산이 백그라운드에서 이루어져, 커밋이 많은 워크스페이스에서도 입력이 멈추지 않습니다.
- 저장소 여러 개를 병렬로 읽어 워크스페이스 로딩이 빨라졌습니다.
- 스크롤 중 그래프와 아바타를 그리는 방식을 최적화해 큰 저장소에서도 목록이 한층 부드럽습니다.
- GitHub Actions 상태가 갱신될 때 화면 전체가 아니라 해당 부분만 다시 그려집니다.
- 큰 diff를 열어 둔 상태에서 화면이 갱신될 때마다 diff를 다시 해석하던 낭비를 없앴습니다.

## 안정성

- 응답 없는 원격 저장소에 가져오기·내려받기가 걸려도 앱이 잠기지 않습니다. 네트워크 작업에 시간 제한이 적용됩니다.
- 충돌 난 rebase를 한 번 완료한 저장소에서 이후 Pull이 계속 거부되던 문제를 고쳤습니다.
- 깨진 저장소가 하나 있어도 워크스페이스의 나머지 저장소는 정상적으로 열리며, 읽지 못한 저장소는 툴바 아이콘으로 안내합니다.
- 작성자 아바타를 찾지 못할 때 커밋마다 조회를 반복하던 문제와, 원격 브랜치가 많은 저장소에서 아바타 조회가 멈추던 문제를 고쳤습니다.
- GitHub API 사용량 한도에 도달하면 안내된 시각까지 자동으로 기다렸다가 다시 시도합니다.
- 자동 가져오기가 반복해서 실패하면 툴바에 표시되어 자격 증명 만료 등을 알아차릴 수 있습니다.
- 브랜치를 빠르게 바꿔 누를 때 이전 선택의 로딩 오류가 뒤늦게 얼럿으로 뜨던 문제를 고쳤습니다.
