# 업데이트 내용

## 자동 가져오기

- 정해 둔 주기마다, 그리고 다른 앱을 쓰다 GitScope로 돌아올 때 모든 원격 저장소를 자동으로 가져옵니다. `⌘R`을 누르지 않아도 브랜치의 앞섬·뒤처짐 수와 원격 브랜치 목록이 최신 상태를 유지합니다.
- 원격에 실제로 변화가 있을 때만 목록을 다시 그립니다. 변화가 없으면 화면이 그대로 유지되어 보던 자리를 잃지 않습니다.
- 가져오는 동안에도 탭 전환과 도구 막대를 그대로 쓸 수 있고, 로딩 화면이 끼어들지 않습니다.
- 네트워크가 끊긴 상황처럼 가져오기에 실패하면 알림 없이 다음 차례에 다시 시도합니다. 실패 내용을 보고 싶으면 `⌘R`로 직접 가져오면 됩니다.
- `⌘,` 설정 창에서 켜고 끄거나 주기를 1·5·15·30분 중에서 고를 수 있습니다. 기본값은 켜짐, 5분입니다.

## 고친 문제

- 커밋 그래프의 노드 동그라미가 왼쪽 가장자리에서 잘려 보이던 문제를 고쳤습니다. 현재 위치를 나타내는 강조 테두리까지 온전하게 보입니다.
