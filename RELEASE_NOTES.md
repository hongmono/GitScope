# 업데이트 내용

## 새 기능

- **브랜치 rebase**: 브랜치 우클릭 메뉴에서 현재 브랜치를 선택한 브랜치 위로 rebase할 수 있습니다. 충돌이 나면 자동으로 되돌려 저장소를 항상 깨끗한 상태로 유지합니다.
- **Pull (Fast-Forward)**: 체크아웃하지 않은 브랜치도 우클릭 한 번으로 원격과 동기화합니다. 빨리 감기가 불가능하면 변경 없이 안내합니다.
- **Push (Upstream 설정)**: upstream이 없는 브랜치를 원격에 게시하면서 upstream을 함께 설정합니다.
- **브랜치·태그 삭제**: 로컬 브랜치, 원격 브랜치, 태그를 사이드바에서 바로 삭제합니다. 모든 삭제는 확인을 거치며, 병합되지 않은 커밋이 있는 브랜치는 한 번 더 경고합니다.

## 개선

- 진행 중인 rebase가 있으면 새 원격 작업을 시작하지 않고 먼저 완료하거나 중단하도록 안내합니다.
- 여러 저장소에 걸친 브랜치는 각 저장소에서 작업을 실행하고, 실패한 저장소만 모아 알려줍니다.
